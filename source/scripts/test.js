/*var scene = getScene();

for (var x = 0; x < 4; x ++) {
	for (var y = 0; y < 4; y ++) {
		for (var z = 0; z < 4; z ++) {
			var test = new Sphere(new vec3(x, y, 100 + z), ((x + y + z) % 7) / 9);
			scene.addObject(test);
		}
	}
}
*/